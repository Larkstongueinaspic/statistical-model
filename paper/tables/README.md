# Tables

These LaTeX tables are compact paper-facing tables derived from generated
outputs under `results/v02/tables/` and `results/v03_gcn/tables/`.

The source-country Top5 comparison table is generated by:

```bash
.venv/bin/python scripts/generate_paper_assets.py
```
